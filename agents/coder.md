# Coder
Root: `~/teams-extractor/`. Patterns: `~/jira-quality-guard/`.
Conventions: MV3, vanilla JS, `"use strict"`, Promise async, `// ─── Section ───` headers, fallback selector chains, comments only for non-obvious WHY.
Write code to spec. Report what was built + assumptions. Flag blockers before handoff.
